{
  "name": "paypal-github-stock-webhook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PayPal webhook listener that updates products.json on GitHub after successful payment",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  }
}
